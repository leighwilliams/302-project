<div id="left">
  <br />
  <h6 class="subheader">Display Modes</h6>
  <a id="BS" class="tiny button radius success">Ball and Stick</a>
  <br />
  <a id="SF" class="tiny button radius">Space Filling</a>
  <br />
  <a id="WF" class="tiny button radius">Wireframe</a>
  <br />
  <input type="checkbox" id="Labs"> <label for="Labs">Labels</label>
</div>
<!-- Scene -->
<div id="right">
  <script>
    // get dimensions
    var width = window.innerWidth - 200;
    var height = window.innerHeight - 71;
    // setup component
    var model = new ChemDoodle.TransformCanvas3D('model', width, height);
    // set the representation to 'Ball and Stick'
    model.specs.set3DRepresentation('Ball and Stick');
    model.specs.macro_displayAtoms = true;
    model.specs.macro_displayBonds = true;
    model.specs.macro_atomToLigandDistance = 5;
    // set the background color to black
    model.specs.backgroundColor = '#111';
    // set to 'cartoon' mode
    model.specs.proteins_ribbonCartoonize = true;
    // load data
    var molFile = pdb_4F5S;
    // set the coordinate multiplier to 1 to stay in Angstroms
    //var molecule = ChemDoodle.readMOL(molFile, 1);
    molFile.residueSpecs = new ChemDoodle.structures.VisualSpecifications();
    molFile.residueSpecs.set3DRepresentation('Wireframe');
    model.loadMolecule(molFile);

    // Click events
    $("#BS").click(function() {
      $(this).addClass("success");
      $("#SF").removeClass("success");
      $("#WF").removeClass("success");
      model.specs.set3DRepresentation('Ball and Stick');
      model.setupScene();
      model.repaint();
    });

    $("#SF").click(function() {
      $(this).addClass("success");
      $("#BS").removeClass("success");
      $("#WF").removeClass("success");
      model.specs.set3DRepresentation('van der Waals Spheres');
      model.setupScene();
      model.repaint();
    });

    $("#WF").click(function() {
      $(this).addClass("success");
      $("#BS").removeClass("success");
      $("#SF").removeClass("success");
      model.specs.set3DRepresentation('Wireframe');
      model.setupScene();
      model.repaint();
    });

    $("#Labs").click(function() {
      model.specs.atoms_displayLabels_3D =! model.specs.atoms_displayLabels_3D;
      model.setupScene();
      model.repaint();
    });
  </script>
</div>
